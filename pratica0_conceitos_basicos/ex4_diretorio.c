/*Escreva e compile o programa abaixo, 
que imprime na tela todos os arquivos da pasta. 
Para isso, chame a operação system("dir") (ou system("ls"), no Linux).

#include <stdlib.h>

int main() {
    system("dir"); // Use "ls" para Linux
    return 0;
}
*/ 

#include <stdlib.h>

int main() {

    system("dir");
    return 0;
}

// sei lá que isso. 